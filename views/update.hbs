<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Profile</title>
    <link rel="stylesheet" href="/dashboard.css">
</head>
<body>
    <div class="sidebar">
        <ul>
            <li><a href="/dashboard">Profile</a></li>
            <li><a href="/update-profile">Update Profile</a></li>
            <li><a href="/semester-registration">Semester Registration</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </div>
    <div class="content">
        <h1>Update Your Profile</h1>
        <form action="/update" method="POST" enctype="multipart/form-data">
            <label for="name">Name:</label>
            <input type="text" name="name" value="{{userDetails.name}}" required>

            

            <label for="branch">Branch:</label>
            <input type="text" name="branch" value="{{userDetails.branch}}" required>

            <label for="year">Year:</label>
            <input type="text" name="year" value="{{userDetails.year}}" required>

            <label for="enrollment">Enrollment Number:</label>
            <input type="text" name="enrollment" value="{{userDetails.enrollment}}" required>

            <label for="mobile">Mobile:</label>
            <input type="text" name="mobile" value="{{userDetails.mobile}}" required>

            <label for="email">Email:</label>
            <input type="email" name="email" value="{{userDetails.email}}" required>

            
            <label for="dob">Date of Birth:</label>
            <input type="date" name="dob" value="{{userDetails.dob}}" required>

            <label for="address">Address:</label>
            <textarea name="address" required>{{userDetails.address}}</textarea>

            <label for="profile_picture">Profile Picture:</label>
            <input type="file" name="profile_picture">

            <button type="submit">Update Profile</button>
        </form>
    </div>
</body>
</html>
